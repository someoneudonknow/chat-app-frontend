import reducer, {
  setConservations,
  setCurrentConservation,
  clearState,
  updateConservation,
  addNewOnlineMember,
  removeOnlineMember,
  addConservation,
} from "./userConservationSlice";

export {
  setConservations,
  setCurrentConservation,
  clearState,
  updateConservation,
  addNewOnlineMember,
  removeOnlineMember,
  addConservation,
};

export default reducer;
